version: 1
services:
  - type: web
    name: app-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    rootDirectory: my-app
    envVars:
      - key: NODE_ENV
        value: production
